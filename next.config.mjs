/** @type {import('next').NextConfig} */
const nextConfig = {
  // Service worker ve manifest public/ klasöründen servis edilir.
  // PWA kaydı istemci tarafında (app/pwa-register.tsx) yapılır.
};

export default nextConfig;
