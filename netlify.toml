[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

# Next.js (App Router) için resmi Netlify eklentisi — SSR/route handler'ları çalıştırır.
[[plugins]]
  package = "@netlify/plugin-nextjs"
